'use client';

import AdminShell from '@/components/admin/AdminShell';

// The admin panel is the home page — no need to navigate to /admin.
export default function Home() {
  return <AdminShell />;
}
